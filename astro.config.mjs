// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({

    // Site URL for sitemap generation, RSS feeds, etc.
    site: "https://www.example.com", // Replace Later On
    trailingSlash: "always",
});
